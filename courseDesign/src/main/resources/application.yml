server:
  port: 80

spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/quickplatform
      username: root
      password: root

mybatis-plus:
  global-config:
    db-config:
      id-type: auto #??id??
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #??????????
